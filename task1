#/bin/bash
#shell script that will print the strings "one","two","three","four","five" on the screnn with each appearing on a separate line
#Create a text file that has each of the these  on a separate line and the file name
#Starts with todays date "yyyy-mm-dd-file.txt".
#Note Dont add todays date manually

printf "%s\n" "one" "two" "three" "four" "five" | tee$(date +%F)-file.txt
